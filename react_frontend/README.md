# Wealth Monitor — Frontend

React + Vite + MUI UI for the training app.

## Setup

```bash
cd react_frontend
npm install
cp .env.example .env
npm run dev
```

Open http://localhost:5173

The API must be running at the URL in `VITE_API_URL`.
